# Prettier-specific overrides — files that ARE tracked by git but should not
# be reformatted by Prettier. Patterns covered by .gitignore (build output,
# node_modules, *.tsbuildinfo, etc.) do NOT need to be listed here because
# the test gate runs Prettier with --ignore-path .gitignore as well.

# Lockfiles (tracked but should not be reformatted)
package-lock.json
yarn.lock
pnpm-lock.yaml

# >>> genesis-managed cloudflare deploy prettier ignores (generated by scripts/Sync-PrettierIgnore.ps1 — edit there) >>>
# Cloudflare Pages deployment infra (injected by the deploy-cloudflare-pages
# components; their YAML/Markdown formatting is sensitive to the resolved Prettier
# version, which drifts without a committed lockfile).
.github/workflows/deploy.yml
docs/deploying-to-cloudflare-pages.md
# <<< genesis-managed cloudflare deploy prettier ignores <<<

# >>> genesis-managed prettier ignores (generated by scripts/Sync-PrettierIgnore.ps1 — edit there) >>>
# Inherited from genesis (not project source — managed via Sync-Instructions.ps1).
.github/instructions/genesis/
.github/ISSUE_TEMPLATE/
.github/copilot-instructions.md
.github/copilot-setup-steps.yml
.github/dependabot.yml
.github/pull_request_template.md
.githooks/
user-features/
AGENTS.md
CLAUDE.md

# Claude Code rules compiled from the Copilot instructions by genesis
# (generated output, not project source — parallels .github/instructions/genesis/).
.claude/rules/generated/

# Genesis scaffolder bookkeeping (records which seed components were applied).
.genesis/

# README.md is auto-generated/maintained by genesis Enrich-Readme.ps1.
README.md
# <<< genesis-managed prettier ignores <<<
